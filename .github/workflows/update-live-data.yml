name: Update live data (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "実況データのGitHub保存は停止中です。"
          echo "現在はブラウザ側で実況を都度取得する構成です。"
